import { observer } from 'mobx-react-lite';
import * as React from 'react';
import { PageHeader } from 'antd';

export const PageDashboard = observer(() => {
  return <>
    <PageHeader title="Главная Десяточки" />
  </>;
});
